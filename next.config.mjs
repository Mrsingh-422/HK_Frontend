/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: [
      "static.vecteezy.com",
      "img.freepik.com",
      "media.istockphoto.com",
    ],
  },
};

export default nextConfig;
